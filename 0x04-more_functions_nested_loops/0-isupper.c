#include "main.h"

/**
* _isupper - a function that checks for uppercase letters
* @c: character
* Return: 1 if uppercase, zero otherwise
*/

int _isupper(int c)
{

if (c >= 65 && c <= 90)
{
return (1);
}
else
{
return (0);
}

}
