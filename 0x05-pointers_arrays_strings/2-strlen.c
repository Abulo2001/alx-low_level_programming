#include <string.h>
#include "main.h"

/**
  * _strlen - a function that returns the lenght of a string
  * @s: pointer argument for string
  * Return: Always 0
  */

int _strlen(char *s)
{
int a;

a = strlen(s);
return (a);
}
